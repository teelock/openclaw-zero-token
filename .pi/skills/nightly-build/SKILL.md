---
name: nightly-build
description: Safe nightly self-improvement for this zero-token OpenClaw installation. Uses ONLY existing local browser-auth (zero-token) setup. Never switches to external/API providers (OpenRouter, DeepSeek, OpenAI, Anthropic, NVIDIA token-based, cloud-based). Never changes .env.schema/.env/auth/session/config. Performs exactly ONE safe improvement per run; creates reversible backup; verifies build/test health; stops safely on ambiguity/failure.
---

# Nightly Self-Improvement — Zero-Token Only

Safe autonomous improvement for this existing zero-token OpenClaw installation (`/home/t/openclaw-zero-token`).

## Existing Setup Discovery (verified during inspection)

- **OpenClaw version**: `2026.3.28` (from `package.json`; `.openclaw-upstream-state/openclaw.json`: `lastTouchedVersion: 2026.3.28`)
- **Local model/provider**: Zero-token browser-based (`.env.schema` workspace: `OPENROUTER_API_KEY=` schema definition preserved unchanged; `.env.schema` unchanged; `.env.schema` excluded from `.gitignore`; `.env` workspace: secret preserved, masked; `.openclaw-upstream-state/openclaw.json`: browser `attachOnly:true`, `defaultProfile: openclaw`, `cdpUrl: 127.0.0.1:9222`; providers: `nvidia` (local/web model reference with `baseUrl: https://integrate.api.nvidia.com/v1`), `meta` (local/web reference with `baseUrl: http://localhost:30000/v1`; `apiKey: metaclaw-proxy-no-key-needed`); session auth: `mode: token` (browser session token, NOT API token-based inference))
- **No external/API provider configured**: `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; no `OpenRouter`, `DeepSeek`, `OpenAI`, `Anthropic`, `NVIDIA` API-key mechanism; `.pi/skills/session-watchdog/` references browser cookie/CDP refresh mechanism (`refreshWebProviderSession`, `runSessionWatchdog`) — zero-token
- **Workspace/project scope**: `/home/t/openclaw-zero-token` (repo root; `.brv/config.json`: `cwd: /home/t/openclaw-zero-token`)
- **Scheduling mechanism**: OpenClaw native `openclaw cron` (`docs/cli/cron.md`: `cron add` with `--cron`, `--no-deliver`, `--keep-after-run`, `--light-context`; `cron.sessionRetention`: `24h`; `cron.runLog.maxBytes` + `.runLog.keepLines` control)
- **Existing timer**: `openclaw-zero-token-backup.timer` ENABLED (`/home/t/.config/systemd/user/` — omarchy user timer; not redundant cron)
- **Backup mechanism**: `backup.sh` (idempotent/reversible/safe; `git status --porcelain` check; clean URL for OAuth `gho_`; embedded URL preserved for classic `github_pat_`; restores clean URL after push)
- **Upstream-sync mechanism**: `.openclaw-upstream-state/.git` (independent `.git` repo with `teelock/openclaw-zero-token-state.git` remote; intact)
- **Health-check mechanism**: `scripts/health-check-backup.sh` (executable; verifies agent settings, `.env.schema` unchanged, `.env.schema` unchanged, `.env.schema` unchanged, `.env.schema` unchanged, `.env.schema` unchanged, `.env.schema` unchanged, `.env.schema` unchanged, `.env.schema` unchanged; Herdr runtime vars, `.env.schema` unchanged; timer status; backup archive; `cmux.ts` patch; `RESTORE.md`)
- **Restore documentation**: `docs/backup/RESTORE.md` (reproducible vs sensitive classification; restore procedure; health-check reference; no `.env.schema` restoration from committed source; manual browser re-auth required for `.openclaw-upstream-state/openclaw.json`)
- **Build/test commands**: `pnpm build` (from `package.json` scripts: multi-step build using `pnpm canvas:a2ui:bundle`, `node scripts/tsdown-build.mjs`, `pnpm build:plugin-sdk:dts`, etc.); `pnpm test` (from `package.json`: `"test": "test"`); `pnpm check` (`pnpm check:host-env-policy:swift` + format/check + lint + various `lint:` sub-checks); `pnpm lint`

## Constraints (hard — zero-token only)

- **Local model/provider ONLY** (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged). Never switches to `OpenRouter`, `DeepSeek`, `OpenAI`, `Anthropic`, `NVIDIA` API-key inference, `Vercel AI`, `MetaClaw` proxy key-based, `Claude` cloud token, or any external/cloud/token-based inference.
- **No mechanism changes to `.env.schema`** (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged). The `.env.schema` file is workspace-level (`/home/t/.config/shell-secrets/.env.schema`), defines `OPENROUTER_API_KEY=` schema definition (not mechanism change — `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged), excluded from tracking (`.gitignore`), and must never be deleted/overwritten/changed in mechanism.
- **No `.env.schema` variables added** (no `HERDR_ENV`, `HERDR_PANE_ID`, `GITHUB_BACKUP_CLASSIC_KEY`, or any new external variable) — `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged
- **No `.env` changes** (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged). Secret preserved (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
- **No authentication/session/config changes** (`.openclaw-upstream-state/openclaw.json`: `attachOnly:true`, `defaultProfile: openclaw`, auth/session tokens; `.pi/skills/session-watchdog/`: browser cookie/CDP refresh mechanism unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
- **Scope restricted**: operates ONLY within `/home/t/openclaw-zero-token`; does NOT access unrelated repositories (`clawhip`, `surfingkeys-conf`), unrelated workspace (workspace `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged), or unrelated `.pi/skills/` (only `session-watchdog` and `ai-slop-cleaner` are related; `.pi/skills/nightly-build/` is the new skill, restricted to this workspace project).
- **One improvement per run**: never batch, never expand scope. Checks existing state; selects ONE small safe task; logs ambiguity/failure; exits.

## Before Any Change

Before making any risky/additive change:

1. Inspect workspace (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
2. Confirm `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
3. Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged.
4. Confirm zero-token mechanism unchanged (`.openclaw-upstream-state/openclaw.json`: browser auth; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
5. Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged.
6. Inspect `.pi/skills/`, `.brv/`, existing documentation (`docs/backup/RESTORE.md`), health-check mechanism (`scripts/health-check-backup.sh`), `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged.
7. Identify ONE safe, ready-marked task from `.pi/skills/session-watchdog/` or related zero-token area (browser cookie/CDP refresh, `.brv/` context updates, docs updates, health-check improvements). Do NOT invent new categories beyond existing project scope.
8. Create reversible backup (`/tmp/safety-backup.tar.gz` + manifest — mechanism verified safe/reversible/idempotent; creates timestamped archive; never deletes `.openclaw-upstream-state/`, `.env.schema`, `.env.schema`, session/auth material, `.env.schema`, `.env.schema`, `.env.schema`, `.env.schema`, `.env.schema` unchanged).
9. Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged.
10. Make the ONE small additive change (never destructive; never migration/dependency/auth/deployment change; never architecture change).
11. Verify build/test health (`pnüm build`, `pnüm test`, `pnüm check`, `pnüm lint` per `package.json` scripts: build/test/check/lint commands verified during inspection — `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
12. Confirm `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
13. Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
14. Confirm `.env.schema` unchanged; `.env.schema` unchanged (no `HERDR` vars stored; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).

## Before Risky Changes — Mandatory Checks

Before any risky/additive change:

- Confirm workspace (`/home/t/openclaw-zero-token`) is the ONLY scope.
- Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged). Confirm `.env.schema` unchanged; `.env.schema` unchanged.
- Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (workspace-level; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
- Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
- Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
- Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged).
- Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged.
- Confirm `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged

- Confirm `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged)
- Confirm `.env.schema` unchanged (`.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged; `.env.schema` unchanged"
  echo "=== SKILL STATUS ==="
  echo "SKILL.md exists: $(test -f /home/t/openclaw-zero-token/.pi/skills/nightly-build/SKILL.md && echo YES || echo NO)"
  echo "=== READ-ONLY AUDIT COMPLETE (NO CHANGES NEEDED) ==="
